{
  "mcpServers": {
    "crm-milestone": {
      "command": "crm-cli",
      "args": ["mcp"],
      "env": {
        "CRM_BASE_URL": "${env:CRM_BASE_URL}",
        "CRM_AUTH_FLOW": "device_code",
        "CRM_TENANT_ID": "${env:CRM_TENANT_ID}"
      }
    }
  }
}
